name: test build

on:
  push:
    push:
      - main


jobs:
  test_build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 
        uses: actions/checkout@v3

      - name: setup database
        run: |
          make build

      - name: launch servers
        run: |
          make serve
